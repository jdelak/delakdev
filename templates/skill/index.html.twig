{% extends 'base.html.twig' %}

{% block title %}Julien Delacourt - Compétences{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-lg-12 col-md-12 mx-auto">
			<h2>Compétences</h2>
			<p>Ancien développeur PHP/Symfony, j'ai décidé en 2019 de devenir développeur JAVA :</p>
			<div class="row">
				<div class="col-lg-6 skill">
					<h3>Java & Co</h3>
					<div class="row">
						{% for javaSkill in javaSkills %}
							<div class="col-lg-4 col-md-4 col-sm-4 col-xs-6 text-center">
								<img  src="{{ asset('images/skills/' ~ javaSkill.image) }}" alt="{{ javaSkill.name }}"/>
								<br>
							</div>
						{% else %}
							<em>no skill found</em>
						{% endfor %}
					</div>
				</div>
				<br>
				<div class="col-lg-6 skill">
					<h3>Frontend</h3>
					<div class="row">
						{% for frontSkill in frontSkills %}
							<div class="col-lg-4 col-md-4 col-sm-4 col-xs-6 text-center">
								<img  src="{{ asset('images/skills/' ~ frontSkill.image) }}" alt="{{ frontSkill.name }}"/>
								<br>
							</div>
						{% else %}
							<em>no skill found</em>
						{% endfor %}
					</div>
				</div>
				<br>
				<div class="col-lg-6 skill">
					<h3>Base de Données</h3>
					<div class="row">
						{% for bddSkill in bddSkills %}
							<div class="col-lg-4 col-md-4 col-sm-4 col-xs-6 text-center">
								<img  src="{{ asset('images/skills/' ~ bddSkill.image) }}" alt="{{ bddSkill.name }}"/>
								<br>
							</div>
						{% else %}
							<em>no skill found</em>
						{% endfor %}
					</div>
				</div>
				<br>
				<div class="col-lg-6 skill">
					<h3>Graphisme</h3>
					<div class="row">
						{% for graphSkill in graphSkills %}
							<div class="col-lg-4 col-md-4 col-sm-4 col-xs-6 text-center">
								<img  src="{{ asset('images/skills/' ~ graphSkill.image) }}" alt="{{ graphSkill.name }}"/>
								<br>
							</div>
						{% else %}
							<em>no skill found</em>
						{% endfor %}
					</div>
				</div>
				<br>
				<div class="col-lg-6 skill">
					<h3>Versionning</h3>
					<div class="row">
						{% for gitSkill in gitSkills %}
							<div class="col-lg-4 col-md-4 col-sm-4 col-xs-6 text-center">
								<img  src="{{ asset('images/skills/' ~ gitSkill.image) }}" alt="{{ gitSkill.name }}"/>
								<br>
							</div>
						{% else %}
							<em>no skill found</em>
						{% endfor %}
					</div>
				</div>


			</div>

		</div>
	</div>
	<hr>
{% endblock %}
